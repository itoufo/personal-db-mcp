SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: API key for multi-tenant mode (generated via web app)
# If not set, falls back to single-profile mode (backward compatible)
PERSONAL_DB_API_KEY=
